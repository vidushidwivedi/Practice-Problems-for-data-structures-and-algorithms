class Solution {
public:
    int reverse(int x) {
        int rev_num= 0;
        while (x){
            rev_num= (rev_num * 10) + x % 10;
            x = x /10;
        }
        return rev_num;
    }
};